---
alwaysApply: true
---

---
alwaysApply: true
---

# 开发规则模板

## 代码风格和规范

### TypeScript/JavaScript
- 使用 TypeScript 严格模式
- 优先使用函数式组件和 React Hooks
- 使用 ESLint 和 Prettier 进行代码格式化
- 组件名使用 PascalCase，文件名使用 kebab-case
- 使用有意义的变量和函数名
- 避免使用 `any` 类型，优先使用具体类型
- 避免存在未使用的变量或导入
- 优先使用es6解构语法

### React 最佳实践
- 使用函数式组件而非类组件
- 合理使用 useMemo 和 useCallback 优化性能
- 避免在 render 中创建新对象或函数
- 使用 React.memo 优化不必要的重渲染
- 组件应该单一职责，保持简洁
- React相关方法应该使用解构的方式导入，如 React.ComponentType需要使用 import { ComponentType } from 'react'

### 文件组织
- 每个组件一个文件夹，包含 index.tsx 和样式文件
- 使用 barrel exports (index.ts) 导出组件
- ui文件夹为存放基础ui组件的地方，站点需要尽可能的使用ui中封装好的组件
- 样式管理使用Tailwind库，除ui中文件外其余文件class均需要用utils中封装好的cn方法，ui中的class管理使用clsx库
- 脚本和组件文件只允许ts或tsx文件
- icons应该封装到assets/icons中，使用tsx的形式保存起来

## 国际化 (i18n)

### 多语言支持
- 所有用户可见文本必须使用国际化
- 使用 useTranslations hook 获取翻译文本
- 翻译键名使用点号分隔的层级结构
- 支持的语言：中文(zh)、英文(en)、日文(ja)
- ui中封装的组件国际化需要通过暴露相应的text props进行适配，禁止在ui中的组件中直接使用类似i18n的翻译库

## 性能优化

### 图片和资源
- 使用 Next.js Image 组件优化图片加载
- 图片使用 WebP 格式，提供 fallback
- 使用适当的图片尺寸和压缩

### 代码分割
- 使用动态导入 (dynamic import) 进行代码分割
- 路由级别的代码分割
- 组件级别的懒加载

## 可访问性 (A11y)

### 语义化 HTML
- 使用正确的 HTML5 语义标签
- 提供适当的 ARIA 标签
- 确保键盘导航支持

### 颜色和对比度
- 确保文本和背景有足够的对比度
- 项目整体颜色风格应该和谐统一
- 遵循WCAG 2.x规范

## 测试

### 集成测试
- 测试关键用户流程
- 测试ts type检测
- 测试构建流程

## 安全

### 依赖管理
- 不要自动更新依赖包
- 使用 pnpm 管理包依赖
- 审查第三方包的安全性

## 文档

### 代码文档
- 为复杂函数添加 @params 注释
- 仅在ui组件需要有 README 文件说明用法和 API
- 保持文档与代码同步

### 项目文档
- 维护清晰的 README
- 记录部署流程
- 记录开发环境设置

### 提交规范
- 使用 Conventional Commits 格式
- 提交信息使用中文
- 每个提交只做一件事

### 分支管理
- 使用 feature 分支开发新功能
- 主分支保持稳定
- 使用 Pull Request 进行代码审查


### 错误边界
- 使用 React Error Boundary 捕获组件错误
- 提供友好的错误页面
- 记录错误信息用于调试

