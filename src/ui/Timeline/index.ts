// Timeline 组件导出
import { Timeline } from 'gemini-uis'

const TimelineItem = Timeline.Item;

export { Timeline, TimelineItem };

export type { 
  TimelineProps, 
  TimelineRef,
  TimelineMode,
  TimelineSize,
  TimelineColor,
  TimelineStatus,
  TimelineItemProps,
  TimelineItemRef
} from 'gemini-uis'